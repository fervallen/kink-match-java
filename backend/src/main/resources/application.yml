server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  # datasource:
  #   url: jdbc:postgresql://localhost:5432/app
  #   username: app
  #   password: app
